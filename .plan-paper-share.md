# 试卷管理：创建人 + 共享设置

对齐题库管理已有的共享模型（`visibleScope` × `shareLevel`），让试卷也能按部门/公司范围共享。

## 已确认的两个语义决策

1. **已发布试卷仍可改共享设置**：可见性与试卷内容正交，锁内容不锁可见性。
   编辑接口只允许草稿态，所以共享设置不能挂在编辑页表单里，需要独立入口。
2. **`view`（可查看）级别的试卷可以被他人用于创建考试**：只读限制的是改试卷本身，
   引用出考试不修改试卷内容，与题库侧 `view` 仍允许抽题组卷的语义一致。

## 一、数据模型

`Paper` 模型补 3 个字段，与 `QuestionBank` 同名同语义同默认值：

- `createBy Int?` —— 创建人 `base_sys_user.id`
- `visibleScope String @default("all")` —— self/dept/company/all
- `shareLevel String @default("manage")` —— manage/view
- 加 `@@index([createBy])`

默认值取 `all`/`manage` 与题库一致，存量试卷 `createBy` 为 NULL、范围为「全部组织」，
迁移后对所有人仍可见可管理，不会突然消失或锁死。

> **实现后的偏差（本节以下内容已不完全反映最终行为）**
>
> 上面说的 DB 列默认值仍是 `all`/`manage`，这部分没变；但**新建试卷的实际默认已改为
> `self` + `manage`**——共享设置从编辑表单移除后，新建不再由前端指定范围，全靠后端
> `normalizeShare` 兜底，若兜成 `all` 则从落库那一刻起组织内任何人都能读到正确答案并能
> 编辑/发布/删除，等于开了一个答案暴露窗口。故应用层默认改为最小可见。
>
> 由此产生两个已知的不一致，均**待定**：
> - DB 列默认值（`all`）与应用层默认（`self`）相反。应用层三处落库入口（含 seed）都显式
>   传值，当前无实际风险；是否加迁移对齐未定。
> - 题库侧共享兜底仍是 `all`/`manage`，与试卷侧现在相反。是否同步收紧未定。
>
> 另一处影响使用流程：非创建人在「选择试卷建考试」的弹窗里看不到别人新建的卷，
> 需创建人先在列表页共享。这与本文档开头第 2 条决策（`view` 级别可被他人用于建考试）
> 不冲突——那条讲的是已共享试卷的级别语义，这里是尚未共享时压根不可见。

迁移走项目既有的 Prisma 流程（改 schema 后 push，CI 自动生成迁移）。

## 二、后端：抽出与实体无关的共享访问服务

现有 `QuestionBankAccessService` 的部门/公司上溯、用户归集、能力位计算逻辑
只依赖 `createBy`/`visibleScope`/`shareLevel` 三个字段，与具体实体无关，
但类型签名绑死了 `Prisma.QuestionBankWhereInput`，试卷无法直接复用。

新增 `services/share-access.service.ts`，承载可复用部分：

- `VISIBLE_SCOPES` / `SHARE_LEVELS` / `AdminPayload` / `isSuperAdmin()`
- `getDeptSubtreeIds()` / `getCompanySubtreeIds()` / `getUserIdsByDepts()`
- `buildVisibleOr(admin)` —— 返回 OR 条件数组，超管返回 `null` 表示不限制；
  只引用 `createBy`/`visibleScope`，两个实体的 WhereInput 结构兼容
- `canRead(record, admin)` / `getCapabilities(record, admin)` —— 入参收窄为
  `{ createBy, visibleScope, shareLevel }` 结构，不关心来自哪张表

`QuestionBankAccessService` 改为委托给它，**公开方法签名与报错文案全部保持不变**，
题库/题目两个控制器 20 余处调用点零改动。

新增 `PaperAccessService`：查 `paper` 表取共享字段，复用上面的能力计算，
报错文案换成试卷语境（「无权访问该试卷」「该试卷为只读共享，无权修改」
「仅试卷创建人可修改共享设置」）。
