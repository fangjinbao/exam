# 题库：创建人 + 共享属性（可见范围 + 权限级别）

## 需求
题库增加**创建人**;增加**共享属性**（仅创建人可改）:
- **可见范围** `visibleScope`：仅自己 / 本部门 / 本公司 / 全部
- **权限级别** `shareLevel`：可管理 / 可查看
  - 可管理 = 题库信息增删改 + 题目增删改查
  - 可查看 = 只读，禁止编辑/删除题库、禁止题目增删改

## 已确认规则
| 规则 | 决定 |
|---|---|
| 存量 4 个题库 | createBy 归 admin，范围=全部，级别=可管理（不打断现有使用） |
| 超管 `username==='admin'` | 通吃：可见+可管理全部题库，含改共享属性 |
| 「可管理」边界 | 能删库、能改题库信息与题目;**改共享属性仅创建人（+超管）** |
| 「本部门」 | 含创建人所属部门的**所有下级子部门** |
| 「本公司」 | 沿 `parentId` 向上找最近 `type ∈ {省公司, 分公司}` 节点，取其整棵子树 |
| 列表 | **按可见范围过滤**，无权题库不返回 |

## 关键现状约束（调研已确认）
1. `QuestionBank` 表无创建人字段;`BaseService`/`CrudControllerBase` **无 where 注入钩子**（`buildWhere` 是 private），题库列表走自定义 `pageWithCount`，过滤需改该方法本身。
2. JWT payload 只有 `{userId, username, roleIds, passwordVersion}`，**无 departmentId** → 需额外查库拿创建人部门。
3. `SysRoleDepartment` 是死表、无 `dataScope`、**无「取部门子树 id」工具** → 需自建。
4. 超管判定现状只有 `username === 'admin'` 硬编码（`perms.guard.ts:63`）。
5. 继承链暗藏两条活路由 `/detail/:id`、`/update-status`，权限收口勿漏。
6. 题目写入口 6+ 条：question.controller 的 add/update/delete/batch-delete/import/audit(approve,reject)。

## 一、数据库（Prisma migration）
`QuestionBank` 增 3 字段：
```prisma
/// 创建人用户 ID（SysUser.id）
createBy      Int?
/// 可见范围 self=仅自己 dept=本部门 company=本公司 all=全部
visibleScope  String @default("all") @db.VarChar(20)
/// 权限级别 manage=可管理 view=可查看
shareLevel    String @default("manage") @db.VarChar(20)

@@index([createBy])
```
- 不加外键关系（与项目现有 `tenantId` 风格一致，避免级联复杂度）;查创建人姓名用单独查询 + map 拼装。
- **迁移数据补齐**：存量记录 `createBy = (admin 用户 id)`、`visibleScope='all'`、`shareLevel='manage'`。migration SQL 里用子查询取 `base_sys_user WHERE username='admin'` 的 id 兜底（取不到则留 NULL）。

## 二、后端

### 新增 `QuestionBankAccessService`（或并入 question-bank.service）
核心能力：
- `getDeptSubtreeIds(deptId)`：递归 `SysDepartment.parentId` 收集本部门+子部门 id（内存递归，一次查全表同租户部门）。
- `getCompanySubtreeIds(deptId)`：沿 parentId 上溯找最近 `type ∈ {省公司,分公司}`，再取其子树 id。
- `buildVisibleWhere(admin)`：产出 Prisma `OR` 条件——
  ```
  超管 → {}（无限制）
  否则 OR: [
    { createBy: userId },                                  // 自己创建的
    { visibleScope: 'all' },                               // 全部可见
    { visibleScope: 'dept',    createBy: { in: 本部门子树用户ids } },
    { visibleScope: 'company', createBy: { in: 本公司子树用户ids } },
  ]
  ```
  注：dept/company 判定的是**创建人所在部门**是否落在当前用户的部门/公司子树内 → 需按部门 id 集合反查 `SysUser.id`（一次 `findMany({where:{departmentId:{in:ids}}, select:{id:true}})`）。
- `assertCanRead(bankId, admin)` / `assertCanManage(bankId, admin)` / `assertCanEditShare(bankId, admin)`：三级断言，越权抛 `ForbiddenException`。
  - canManage = 超管 ‖ 创建人 ‖ (可见 && shareLevel==='manage')
  - canEditShare = 超管 ‖ 创建人

### 改造点
1. **list / export**：`pageWithCount` 与 `exportList` 的 where 合并 `buildVisibleWhere`;返回体补 `createBy / createByName / visibleScope / shareLevel / canManage / canEditShare`（后两个为前端按钮控制用的计算字段）。
2. **add**：写入 `createBy = admin.userId`;接受 `visibleScope / shareLevel`（默认 all/manage）。
3. **update**：先 `assertCanManage`;若请求体带 `visibleScope`/`shareLevel` 且与库中值不同 → 追加 `assertCanEditShare`。
4. **delete / batchDelete**：逐个 `assertCanManage`（批量里有一个越权即整体拒绝并提示）。
5. **detail / update-status**（继承链路由）：覆写并加 `assertCanRead` / `assertCanManage`。
6. **import**（题库导入）：新建的库同样写 `createBy`。
7. **题目侧收口**（question.controller / service）：`add/update/delete/batch-delete/import/audit-approve/audit-reject` 均需按 `questionBankId` 调 `assertCanManage`;题目 `list/detail` 调 `assertCanRead`。
   - 注意 `Question.questionBankId` 可空 → 为空时按现状放行（存量兼容），不做拦截。

### DTO / VO
- `CreateQuestionBankDto` / `UpdateQuestionBankDto` 增 `visibleScope?`、`shareLevel?`（`@IsIn` 校验枚举值）。
- `QuestionBankVo` 增 `createBy, createByName, visibleScope, shareLevel, canManage, canEditShare`。

## 三、前端（admin）
1. **列表页** `views/question-bank/list/index.vue`：
   - 表格增列：**创建人**（createByName）、**共享**（一列展示"范围 + 级别"两个 ElTag，如 `本部门` `可管理`）。
   - 筛选区增：可见范围下拉、"仅看我创建的"开关（前端传参，后端 where 追加）。
   - 按钮态：`编辑`/`删除`/`批量删除` 依据行 `canManage` 禁用或隐藏;`进入题库` 始终可点（进去后内部按 canManage 控制题目按钮）。
2. **新增/编辑弹窗**（内联在同文件）：
   - 增「共享设置」两个字段：可见范围 ElSelect + 权限级别 ElRadio（可管理/可查看）。
   - 编辑态：`canEditShare === false` 时这两项**禁用并附提示**"仅创建人可修改共享设置"。
3. **题目页** `views/question-bank/questions/index.vue`：
   - 进入时拿题库 `canManage`;为 false 则隐藏/禁用 新增题目、编辑、删除、批量删除、导入、审核 等写操作按钮，页面顶部给一条只读提示。
4. **API** `api/questionBank.ts`：类型与参数补新字段。

## 四、涉及文件
**后端**
- `server/prisma/schema.prisma` + 新 migration（含存量数据补齐 SQL）
- `server/src/modules/exam/services/question-bank.service.ts`（过滤 + 断言 + createBy 写入）
- 新增 `server/src/modules/exam/services/question-bank-access.service.ts`（部门/公司子树 + 三级断言）
- `server/src/modules/exam/controllers/admin/question-bank.controller.ts`（含覆写 detail / update-status）
- `server/src/modules/exam/controllers/admin/question.controller.ts` + `services/question.service.ts`（题目写操作收口）
- `dto/question-bank.dto.ts`、`vo/question-bank.vo.ts`
- exam module 的 providers 注册

**前端**
- `admin/src/views/question-bank/list/index.vue`
- `admin/src/views/question-bank/questions/index.vue`
- `admin/src/api/questionBank.ts`

## 五、验证
- 后端 `npm run build`;前端 `npm run build`（含 vue-tsc）。
- migration 在开发库执行并核对存量 4 个题库已归属 admin。
- 手测矩阵（至少覆盖）：
  1. admin 建库设「仅自己」→ 换普通用户登录，列表看不到。
  2. 设「本部门/可查看」→ 同部门用户能看到，编辑/删除按钮不可用，题目页无写操作;直接调 API 也应 403。
  3. 设「本公司/可管理」→ 同公司跨部门用户能改题库和题目，但改不了共享设置（403 + 前端禁用）。
  4. 「全部」→ 所有人可见。
  5. 超管始终可见可管。
- 完成后删除本 .plan 文件;改动交后台 code-reviewer 审查。

## 六、先修的历史遗留（本轮附带，来自上轮审查）
上一轮考试向导有两个阻断缺陷，一并修掉：
1. **CRITICAL** `CandidatePickerDialog.vue`：`toggleRowSelection` 未传 `emitChange=false` + `handleSelect` 盲翻转 → 清空/移除/回填被静默撤销。改为同步调用传 `false`，且 `handleSelect` 依据事件真实 `sel` 数组判定。
2. **HIGH** `selectAllFiltered` 传 `pageSize:9999` 但服务端硬上限 100 静默截断 → 改为按 100 分页循环拉满 `total`。

## 七、说明与建议
- 「本部门/本公司」判定的是**创建人**所在部门与**当前访问者**部门子树的交集关系，因此需要按部门集合反查用户 id 集合。若后续用户量很大（万级），这个 `in` 集合会偏大，届时可改为在 `QuestionBank` 上冗余 `createDeptId` 字段直接比对部门 id，避免反查用户表——现阶段（企业内部考试平台）先用反查实现，逻辑更直观且无需维护冗余字段一致性。
- 你上一条提的**整页视觉美化**我还没做（问风格方向时被打断了）。本轮先落这个功能需求，美化等你定风格后单独一轮做，避免功能改动和大面积样式改动混在一起难以回溯。
